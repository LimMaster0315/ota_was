services:
  ota-server:
    build: .
    container_name: ota-download-server
    restart: unless-stopped
    ports:
      - "80:8000"
    environment:
      DJANGO_DEBUG: "false"
      DJANGO_ALLOWED_HOSTS: "*"
      DJANGO_SECRET_KEY: "change-this-secret-key"
      OTA_FILE_ROOT: "/app/ota_files"
      OTA_DOWNLOAD_MAP: '{"ml":"ml/ML3_T7_R2.bin"}'
    volumes:
      - ${HOST_OTA_FILE_ROOT:-./ota_files}:/app/ota_files:ro
